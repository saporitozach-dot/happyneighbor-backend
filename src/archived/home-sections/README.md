# Archived homepage sections

Removed from the landing page so the home scroll stays focused: hero → problem/stats → CTA.

| File | Former section |
|------|----------------|
| `FeaturesSection.jsx` | **Why BlockParty** — four feature cards |
| `ProductPreview.jsx` | **The block hub** — “See what your neighbors see” + demo bento |

## Reuse

```jsx
import FeaturesSection from "../archived/home-sections/FeaturesSection";
import ProductPreview from "../archived/home-sections/ProductPreview";
```

Good fits: `/how-it-works`, `/about`, or a dedicated product tour page.
